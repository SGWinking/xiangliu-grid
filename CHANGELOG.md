# 更新日志 · 相柳网格 Xiangliu Grid

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的写法。

**0.5.1 及更早的历史**保留在本仓库 `README.md` 的「更新日志 / Changelog」章节里
（那份是双语长文，没有搬过来）。

---

## [0.6.0] — 2026-09-23

「大云壁画工具箱」系列化交付版本。**图像算法一行未改**，改的全是外壳与安全。

### 修复 —— 按 2026-07-18 改版规格里的 6 条 P0 逐条落实

#### P0-1 静态文件路径越界（安全问题）

旧版 GET 回退直接拼 `WEB_ROOT / parsed.path`，`GET /..\server.py` 能把完整源码读出去。
`/outputs/` 前缀更糟 —— 它把 `ROOT` 当根，`/outputs/../server.py` 同样能读到源码。

现在所有静态、输出、资源文件都必须经过 `toolkit_core.safe_join()` 的真路径包含校验，
越界一律 403，并且错误详情不回显服务端的绝对路径。

#### P0-2 切图参数能把服务拖死

旧版对这些参数完全不设防：`overlap >= 块边长` 时步长会被静默压成 1，
一张 2048×2048 的图配上「块尺寸 256 / 重叠 256」就能推导出上百万个图块，
接口实测超时，服务被拖死。

现在 `/api/plan`、`/api/split`、`/api/grid-plan`、`/api/split-grid`、`/api/resize-export`
全部走统一的范围校验（`clamp_int` / `clamp_float`），并新增两条跨字段校验：

- `check_overlap()` —— 重叠必须小于块边长；
- `check_tile_budget()` —— 单次最多切出 20000 块，工作画布不超过 40 亿像素。

越界返回 400 + 可直接显示给用户的中文说明。

#### P0-3 file:// 兼容不完整

补上 `OPTIONS` 预检（返回 204）与受限 CORS：只放行 `http://127.0.0.1:*`、
`http://localhost:*` 和 `Origin: null`，其它来源不发放 CORS 头。

#### P0-4 「局部回贴」功能不可达

`patchPage` 与完整的事件代码一直都在，但导航只有四个标签，这个页面没有任何入口，
属于不可达功能。现在导航补上「2 局部回贴」，并按要求重排：

```text
图块裁切 → 局部回贴 → 整图拼合 → 行列裁切 → 分辨率导出
```

#### P0-5 上传与输出缺少资源与覆盖保护

- `/api/upload` 增加 2 GB 体积上限，超限返回明确的中文提示；
- 同名文件**不再覆盖**，自动追加 `_1` / `_2` 后缀（`core.unique_path`）；
- 上传文件名统一走 `core.safe_filename`（保留中文、过滤非法字符）；
- JSON 请求体增加 1 MB 上限；
- `serve_file()` 不再 `read_bytes()` 把整文件读进内存，改为 1 MB 分块传输。

#### P0-6 源码与打包版本不一致

版本号收敛为**唯一来源**：`server.py` 里的 `APP_VERSION = "0.6.0"`。

此前同时存在四个互相矛盾的版本：启动脚本印 `v0.3.0`、服务器 `0.5.0`、
README `0.5.1`、使用说明 `v0.4.1`。现在页头、README、CHANGELOG、
启动脚本全部由这一处驱动或与之对齐。启动脚本也不再写死某台机器上的
Python 绝对路径（那行 `C:\Users\ZH\.cache\codex-runtimes\...` 换机器就废）。

### 新增

- 公共底座 `toolkit_core.py`：路径安全、受限 CORS、分块传输、统一错误结构、版本一致。
- `GET /api/health` 健康检查，供工具箱启动台探活，也让启动脚本能判断服务是否真的起来了。
- 统一启动脚本 `run.bat` + `run.ps1`：自动定位 Python、检查依赖、健康检查、自动打开浏览器。
  `run.ps1 -NoBrowser` 只起服务不开浏览器，便于自动化验证。
- `.gitignore` 补齐运行产物与打包产物。
- `requirements.txt`：Pillow / numpy 为必需，opencv-python 为可选增强
  （装了走 cv2 LANCZOS4，千万像素级大图快很多；不装自动退回 Pillow）。

### 变更

- `APP_NAME` 改为中文「相柳网格」，启动横幅统一为系列的 `print_banner` 格式。
- 错误响应统一为 `{"error": {code, message, field, detail}}`；
  前端新增 `errText()` 解析，能直接把中文 `message` 显示给用户。
  **成功响应保持原有字段不变**，不破坏任何既有调用。

### 未改动（刻意为之）

- **全部图像算法逐行未动**：切块、拼合、接缝平衡、低频颜色场校正、
  18 色网格矫正、色卡白平衡、Lab Reinhard、羽化遮罩、结构重上色，
  以及 `color_engine/` 与 `experiments/` 下所有模块，**一个数值都没改**。
- **界面视觉未动**：相柳的视觉是本系列的基准，本次只补了一个导航入口、
  改了页头的版本号文字，其余样式与布局原样保留。

### 已知待办

1. `web/index.html` 仍是单文件内联 CSS/JS（1854 行）。改版规格 §7 建议拆成
   `index.html` + `styles.css` + `app.js` + `workflows/*.js`。
   本次**没有拆** —— 那是纯结构重构，对功能零收益、风险却不小，留作后续独立进行。
2. `/api/preview` 允许按任意本机绝对路径读取图片。这是本地工具的有意设计
   （用户要能预览自己机器上的任意图），但确实意味着本机进程能借这个接口读文件。
   若要收紧，得先改成「只允许已登记的原图与输出」白名单。
3. 分辨率导出虽已限制到 16 倍并在提交前显示目标尺寸，但还没有「预计内存占用」提示。

---

## 0.5.1 及更早

见 [README.md](README.md) 的「更新日志 / Changelog」章节。
